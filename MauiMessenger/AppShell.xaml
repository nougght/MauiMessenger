<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiMessenger.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiMessenger.Views"
    Title="MauiMessenger"
     NavBarIsVisible="False">

    <!--<FlyoutItem Title="Чаты" >
        <ShellContent ContentTemplate="{DataTemplate local:ChatsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Контакты" >
        <ShellContent ContentTemplate="{DataTemplate local:ContactsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Настройки" >
        <ShellContent ContentTemplate="{DataTemplate local:SettingsPage}" />
    </FlyoutItem>-->

    <!--<TabBar>
        <ShellContent Title="Чаты" ContentTemplate="{DataTemplate local:ChatsPage}" />
        <ShellContent Title="Настройки" ContentTemplate="{DataTemplate local:SettingsPage}" />
        <ShellContent Title="Контакты" ContentTemplate="{DataTemplate local:ContactsPage}" />
    </TabBar>-->

</Shell>
